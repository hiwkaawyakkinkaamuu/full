name: Docker Development (UAT)

on:
  push:
<<<<<<< HEAD
    branches:
      - develop
    paths:
      - "backend/**"
      - "frontend/**"
      - "k8s/**"
      - ".github/workflows/docker-development.yml"
  pull_request:
    branches:
      - develop
    paths:
      - "backend/**"
      - "frontend/**"

jobs:
  backend-ci:
    name: Backend (Go) CI
    runs-on: self-hosted
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: backend/go.mod
          cache-dependency-path: backend/go.sum

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test ./...

      - name: Build backend
        run: go build -o main ./main.go

  frontend-ci:
    name: Frontend (Next.js) CI
    runs-on: self-hosted
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint || true

      - name: Build frontend
=======
    branches: [develop]

jobs:

  backend-ci:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'

      - run: go mod download
      - run: go test ./...
      - run: go build -o main ./main.go


  frontend-ci:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci
      - run: npm run lint || true

      - name: Build
>>>>>>> develop
        env:
          NEXT_PUBLIC_API_URL: /api
        run: npm run build

<<<<<<< HEAD
  build-and-push-uat:
    name: Build and Push Images (UAT)
    runs-on: self-hosted
    needs:
      - backend-ci
      - frontend-ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: backend
            context: ./backend
            dockerfile: ./backend/Dockerfile
          - service: frontend
            context: ./frontend
            dockerfile: ./frontend/Dockerfile
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push ${{ matrix.service }} image for UAT
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/p2-final-project-youth-team-${{ matrix.service }}:develop
            ${{ secrets.DOCKER_USERNAME }}/p2-final-project-youth-team-${{ matrix.service }}:develop-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-uat:
    name: Deploy to UAT Kubernetes
    runs-on: self-hosted
    needs:
      - build-and-push-uat
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure namespace
        run: kubectl get namespace uat || kubectl create namespace uat

      - name: Apply manifests
=======

  build-and-push-uat:
    runs-on: ubuntu-latest
    needs: [backend-ci, frontend-ci]

    strategy:
      matrix:
        service: [backend, frontend]

    steps:
      - uses: actions/checkout@v4

      - name: Login Docker
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build image
        run: |
          docker build -t tonglongridz/p2-final-project-youth-team-${{ matrix.service }}:develop ./$(echo ${{ matrix.service }})

      - name: Push image
        run: |
          docker push tonglongridz/p2-final-project-youth-team-${{ matrix.service }}:develop


  deploy-uat:
    runs-on: self-hosted
    needs: build-and-push-uat

    steps:
      - uses: actions/checkout@v4

      - name: Check Kubernetes
        run: |
          kubectl version --client
          kubectl get nodes

      - name: Install NGINX Ingress Controller
        run: |
          if ! kubectl get ns ingress-nginx >/dev/null 2>&1; then
            kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/cloud/deploy.yaml
            echo "Installing ingress-nginx..."

            kubectl rollout status deployment/ingress-nginx-controller -n ingress-nginx --timeout=300s
          else
            echo "Ingress already installed"
          fi

      - name: Create namespace
        run: |
          kubectl get namespace uat || kubectl create namespace uat

      - name: Deploy manifests
>>>>>>> develop
        run: |
          kubectl apply -n uat -f k8s/postgres.yaml
          kubectl apply -n uat -f k8s/pgadmin.yaml
          kubectl apply -n uat -f k8s/backend.yaml
          kubectl apply -n uat -f k8s/frontend.yaml
          kubectl apply -n uat -f k8s/ingress.yaml

<<<<<<< HEAD
      - name: Update deployment images
        run: |
          kubectl set image -n uat deployment/backend backend=${{ secrets.DOCKER_USERNAME }}/p2-final-project-youth-team-backend:develop
          kubectl set image -n uat deployment/frontend frontend=${{ secrets.DOCKER_USERNAME }}/p2-final-project-youth-team-frontend:develop

      - name: Rollout restart
        run: |
          kubectl rollout restart -n uat deployment/backend
          kubectl rollout restart -n uat deployment/frontend

      - name: Verify rollout
        run: |
          kubectl rollout status -n uat deployment/backend
          kubectl rollout status -n uat deployment/frontend
=======
      - name: Update images
        run: |
          kubectl set image deployment/backend backend=tonglongridz/p2-final-project-youth-team-backend:develop -n uat
          kubectl set image deployment/frontend frontend=tonglongridz/p2-final-project-youth-team-frontend:develop -n uat

      - name: Verify
        run: |
          kubectl rollout status deployment/backend -n uat
          kubectl rollout status deployment/frontend -n uat
>>>>>>> develop
          kubectl get pods -n uat